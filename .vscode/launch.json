{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch La Vie en Rose",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Angular: serve"
    }
  ]
}
